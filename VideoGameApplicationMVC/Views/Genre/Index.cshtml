@model List<GenreViewModel>;

<div class="container">

	@foreach(var genre in Model)
	{
	<div class="row">
			<h5 class="text-center text-gray">@genre.Name</h5>
			@foreach(var game in genre.Games.Take(5))
			{
				<p class="text-center">@game.Name</p>
			}
	</div>
	}
